models:
  PageTravels:
    meta_title: json
    meta_desc: json
    header_heading: json
    heading: json
    banner: text
    relationships:
      hasMany: PageTravelsBlock

  PageTravelsBlock:
    image: text
    text: json
    sort: integer nullable
    relationships:
      belongsTo: PageTravels
